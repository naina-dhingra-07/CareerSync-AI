export const analyzeSkillGap = async (req: any, res: any, next: any) => {
  res.status(501).json({ message: 'Moved to Firestore' });
};

export const getMySkills = async (req: any, res: any, next: any) => {
  res.status(501).json({ message: 'Moved to Firestore' });
};

export const markSkillComplete = async (req: any, res: any, next: any) => {
  res.status(501).json({ message: 'Moved to Firestore' });
};

export const getUserSkills = async (req: any, res: any, next: any) => {
  res.status(501).json({ message: 'Moved to Firestore' });
};
